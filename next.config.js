/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/virtudes",
  assetPrefix: "/virtudes/",
  output: "export",
  images: { unoptimized: true },
};

module.exports = nextConfig;
